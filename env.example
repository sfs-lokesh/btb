# MongoDB Connection String
# Replace this with your actual MongoDB connection string
# 
# For MongoDB Atlas (Cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/behind-the-build?retryWrites=true&w=majority
#
# For Local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/behind-the-build
#
# IMPORTANT: If you're getting ECONNRESET errors:
# 1. For Atlas: Check Network Access settings and whitelist your IP
# 2. For Local: Make sure MongoDB service is running
# 3. Test your connection string format

MONGODB_URI=mongodb://localhost:27017/behind-the-build

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
